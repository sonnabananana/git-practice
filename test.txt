git config --global --edit : First time, save username settings

git init : Initialise a gift repository

git add [filename] : "stage" a file so it is ready to save
git commit -m "Message" : "commit" (i.e. Save) the files you staged